# Local dev: optional copy to `.env` in this folder (`apps/web`).
# Default setup needs no variables — Vite proxies `/api` to the server (see vite.config.ts).

# If you later point the UI at a different API origin:
# VITE_API_ORIGIN=http://localhost:3000
