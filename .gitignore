# Dependencies
node_modules/

# Build
dist/
build/
*.tsbuildinfo

# Env / secrets (keep *.example committed for local dev templates)
.env
.env.*
!.env.example
!**/.env.example

# Local SQLite databases (dev / smoke tests)
apps/server/data/
data/

# Brainstorming visual companion (if used)
.superpowers/

# Git worktrees (implementation isolation)
.worktrees/

# Local Xray binary for `bun run vless-test` (see scripts/vless-test.ts)
tools/xray/

# OS / editor
.DS_Store
Thumbs.db

# Cursor agent debug ingest logs (can contain hosts/ports from local runs)
.cursor/debug-*.log
